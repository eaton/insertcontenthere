<!doctype html>
<html lang="en">
	{% include "components/head.njk" %}
	<body>
		<header>
			<em>{% if '/' == page.url %}{{ metadata.title }}{% else %}<a href="{{ '/' | url }}">{{ metadata.title }}</a>{% endif %}</em>
		</header>

		<main>
			{{ layoutContent | safe }}
		</main>
		{% include "components/footer.njk" %}
	</body>
</html>
